<h1>
  <%= @genre.name %>
</h1>
<h2>
  <% @genre.artists.each do |a| %>
  <a href="/artists/<%= a.slug %>"><%= a.name %>
  <% end %>

  <% @genre.songs.each do |s| %>
  <a href="/songs/<%= s.slug %>"><%= s.name %>
  <% end %>
</h2>
